/* Plugin generated by AMXX-Studio */

#include <amxmodx>
#include <fun>
#include <expmod>

#define PLUGIN "ExpMod Shop: Health"
#define VERSION "1.0"
#define AUTHOR "GT Team"

new iItem;

public plugin_init() {
	register_plugin(PLUGIN, VERSION, AUTHOR)
	
	iItem = exp_shop_register_item("100 HP", "Dostajesz 100 HP", 15, 0, 0)
}

public exp_shop_item_selected(id, item) 
	if(item == iItem)
		set_user_health(id, get_user_health(id) + 100)
